from pydantic import BaseModel

class AlertCount(BaseModel):
    critical: int
    medium: int
    low: int
from pydantic import BaseModel

class AlertCount(BaseModel):
    critical: int
    medium: int
    low: int